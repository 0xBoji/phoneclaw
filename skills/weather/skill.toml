[metadata]
name = "weather"
version = "0.1.0"
description = "Weather lookup workflow using web_search and local time context."
always = false

[permissions]
tools = ["web_search", "datetime_now"]
fs_scope = "workspace"
network_domains = ["api.search.brave.com"]
